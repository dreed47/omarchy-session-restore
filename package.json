{
  "name": "omarchy-session-restore",
  "version": "2.0.3",
  "description": "Save your open apps and window layout, and reopen them automatically after a reboot. Omarchy shell plugin for Hyprland.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-manifest.mjs",
    "check": "node --check restoreLogic.mjs && node --check lib/io.mjs && node --check bin/session-restore && node scripts/validate-manifest.mjs && node --test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dreed47/omarchy-session-restore.git"
  },
  "engines": {
    "node": ">=18"
  }
}
